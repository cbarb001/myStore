<h1>New product</h1>

<%= form_for(@product) do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.label :description %>
	<%= f.text_area :description, rows: 6 %>

	<%= f.label :price %>
	<%= f.text_field :price %>

	<%= f.submit %>
<%end%>